<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mage.testuidemo">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestUIDemo">
        <activity android:name=".activity.CustomDrawableActivity"></activity>
        <activity android:name=".activity.ShadowActivity" />
        <activity android:name=".activity.Matrix1Activity" />
        <activity android:name=".activity.MatrixActivity" />
        <activity android:name=".view.ActCamera" />
        <activity android:name=".activity.MatrixCameraActivity" />
        <activity android:name=".activity.ThirdActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>